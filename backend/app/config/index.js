const config = {
    
    app: {
        port: process.env.PORT || 3000,
    },

    db:{
        db_name:"luan_van",
        user_name:"root",
        pass_word:''
    }
};
    
module.exports = config;